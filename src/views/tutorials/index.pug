extends ../layout

block layout-content
    div
        h1 #{title}

        p #{message}

        ul
            each tutorial in tutorials
                li
                    a(href='/tutorials/' + tutorial.id)= tutorial.id + ': ' + tutorial.title
